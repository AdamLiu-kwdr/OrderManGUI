export const environment = {
  production: true,
  //Always use Server's IP address. Do not use localhost (Don't worry, local machine will reslove automatically.)
  OrderManApiURL:'(connecction address)',
};
